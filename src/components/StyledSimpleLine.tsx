import { styled } from "@mui/material";

const SimpleLine = () => {

    const SimpleLine = styled("div")(({})=>({
        background: "#FFF",
        width: "100%",
        height: "4px",
    }))

    return (
    <>
        <SimpleLine/>
    </>
  )
}

export default SimpleLine;
